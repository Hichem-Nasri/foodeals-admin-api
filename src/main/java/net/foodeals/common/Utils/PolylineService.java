package net.foodeals.common.Utils;

import net.foodeals.common.valueOjects.Coordinates;

public class PolylineService {
    public static String generate(Coordinates from, Coordinates to) {
        return "mock_encoded_polyline"; // future: remplacer par appel réel Google Maps
    }
}
